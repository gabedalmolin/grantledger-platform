apiVersion: 1

datasources:
  - name: GrantLedger Prometheus
    uid: grantledger-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
